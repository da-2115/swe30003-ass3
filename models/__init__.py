"""Make the `models` directory a Python package so other apps can import
the project's model definitions directly (e.g. `from models.Product import Product`).

This file is intentionally minimal.
"""
